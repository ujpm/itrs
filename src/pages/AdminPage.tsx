import { Box, Typography } from '@mui/material';

export default function AdminPage() {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4" mb={3}>Admin Dashboard</Typography>
      {/* TODO: List/filter all complaints, update status, respond */}
    </Box>
  );
}
